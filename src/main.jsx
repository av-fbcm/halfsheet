import { StrictMode } from "react";
import ReactDOM from "react-dom/client";
import HalfSheetGenerator from "./HalfSheetGenerator";

ReactDOM.createRoot(document.getElementById("root")).render(
  <StrictMode>
    <HalfSheetGenerator />
  </StrictMode>
);
